let createConnection,createPool;_b8c‍.x([["db_conn",()=>db_conn],["connection",()=>connection]]);_b8c‍.w("mysql2/promise",[["createConnection",["createConnection"],function(v){createConnection=v}],["createPool",["createPool"],function(v){createPool=v}]]);

let connection;

const db_conn = async () => {
  _b8c‍.u(connection = await createConnection({
    host: "localhost",
    user: "root",
    password: "",
    database: "bincom",
  }));

  // let [rows] = await connection.execute("select * from party");
  // console.log(rows);
};

// db_conn.connect((err) => {
//   if (err) {
//     return console.error("MySql: Database Connection failed");
//   }

//   console.log(`Mysql: Database connection successful`);
// });


